# /etc/nginx/conf.d/{{ item }}.conf
# Generated by Ansible for {{ ansible_facts['fqdn'] }}.

# Enable ETag HTTP header support?
etag on;

# When will a response expire?
# Google recommends 1 year, set this to whatever makes sense.
expires 4w;
