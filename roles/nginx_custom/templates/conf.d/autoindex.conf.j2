# /etc/nginx/conf.d/{{ item }}.conf
# Generated by Ansible for {{ ansible_facts['fqdn'] }}.
# Enables listing of directories when no index is present
autoindex            on;
autoindex_exact_size off;
